# Citizen Verification Portal

Public route: `/citizen`

## Features

- Medicine serial verification (`POST /api/v1/public/verify/`)
- Counterfeit reporting
- Public recall listing
- Trusted pharmacy discovery

Rate-limited on backend (`citizen` throttle scope). No login required.
