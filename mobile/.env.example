# NPTTE Mobile — environment (copy to .env.local for local overrides)
EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
EXPO_PUBLIC_API_TIMEOUT_MS=30000

# EAS (Expo Application Services)
# EAS_PROJECT_ID=
# EXPO_OWNER=

# Staging / production builds set APP_ENV via eas.json profiles
