from django.apps import AppConfig


class BlockchainBridgeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.blockchain_bridge"
    verbose_name = "Blockchain Bridge"
